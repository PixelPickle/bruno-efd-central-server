info:
  name: Read Puzzle Prerequisite
  type: http
  seq: 2

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzlePrerequisite/:puzzleLibraryId/:prerequisitePuzzleLibraryId"
  params:
    - name: puzzleLibraryId
      value: "2"
      type: path
    - name: prerequisitePuzzleLibraryId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
