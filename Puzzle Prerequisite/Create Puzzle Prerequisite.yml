info:
  name: Create Puzzle Prerequisite
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzlePrerequisite"
  body:
    type: json
    data: |-
      {
        "puzzleLibraryId": "2",
        "prerequisitePuzzleLibraryId": "2",
        "name": "Something"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
