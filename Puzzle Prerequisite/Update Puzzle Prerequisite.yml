info:
  name: Update Puzzle Prerequisite
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzlePrerequisite/:puzzleLibraryId/:prerequisitePuzzleLibraryId"
  params:
    - name: puzzleLibraryId
      value: "2"
      type: path
    - name: prerequisitePuzzleLibraryId
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Something new"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
