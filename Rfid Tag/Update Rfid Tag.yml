info:
  name: Update Rfid Tag
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTag"
  body:
    type: json
    data: |-
      {
        "id": "94581046",
        "rfidTagTypeId": 6
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
