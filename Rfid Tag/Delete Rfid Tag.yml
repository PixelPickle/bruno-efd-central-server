info:
  name: Delete Rfid Tag
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTag/:rfidTagId"
  params:
    - name: rfidTagId
      value: "1234567890"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
