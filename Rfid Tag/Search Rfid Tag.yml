info:
  name: Search Rfid Tag
  type: http
  seq: 5

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTag"
  params:
    - name: RFIDTAGTYPEID
      value: "5"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
