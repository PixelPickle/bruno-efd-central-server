info:
  name: Create Rfid Tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTag"
  body:
    type: json
    data: |-
      {
        "id": "1234567890",
        "rfidTagTypeId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
