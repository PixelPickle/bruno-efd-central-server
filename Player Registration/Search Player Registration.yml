info:
  name: Search Player Registration
  type: http
  seq: 4

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/playerRegistration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
