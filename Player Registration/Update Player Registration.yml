info:
  name: Update Player Registration
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/playerRegistration/:playerRegistrationId"
  params:
    - name: playerRegistrationId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Something Else"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
