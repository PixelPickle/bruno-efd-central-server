info:
  name: Create Puzzle Library
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzleLibrary"
  body:
    type: json
    data: |-
      {
        "name": "Puzzle Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
