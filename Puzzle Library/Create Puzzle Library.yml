info:
  name: Create Puzzle Library
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzleLibrary"
  body:
    type: json
    data: |-
      {
        "name": "Puzzle Name",
        "roomId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
