info:
  name: Delete Puzzle Library
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzleLibrary/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
