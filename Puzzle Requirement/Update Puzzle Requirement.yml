info:
  name: Update Puzzle Requirement
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/puzzleRequirement/:puzzleLibraryId/:solutionLibraryId"
  params:
    - name: puzzleLibraryId
      value: "2"
      type: path
    - name: solutionLibraryId
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Changed Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
