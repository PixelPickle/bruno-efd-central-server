info:
  name: Create Solution Library
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/solutionLibrary"
  body:
    type: json
    data: |-
      {
        "name": "Solution A",
        "roomId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
