info:
  name: Create Game Status
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/gameStatus"
  body:
    type: json
    data: |-
      {
        "name": "TEST_GAME_STATUS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
