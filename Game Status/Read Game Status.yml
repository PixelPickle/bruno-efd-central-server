info:
  name: Read Game Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/gameStatus/:gameStatusId"
  params:
    - name: gameStatusId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
