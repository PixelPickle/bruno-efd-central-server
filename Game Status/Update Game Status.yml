info:
  name: Update Game Status
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/gameStatus/:gameStatusId"
  params:
    - name: gameStatusId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "NEW_GAME_STATUS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
