info:
  name: Create Lakelis Question
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/lakelisQuestion"
  body:
    type: json
    data: |-
      {
        "text": "Lakelis Question Text",
        "requiredAmountOfCoupons": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
