info:
  name: Read Lakelis Question
  type: http
  seq: 2

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/lakelisQuestion/:lakelisQuestionId"
  params:
    - name: lakelisQuestionId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
