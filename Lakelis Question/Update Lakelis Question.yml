info:
  name: Update Lakelis Question
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/lakelisQuestion/:lakelisQuestionId"
  params:
    - name: lakelisQuestionId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "text": "Lakelis Question Text",
        "requiredAmountOfCoupons": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
