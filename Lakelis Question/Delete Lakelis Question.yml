info:
  name: Delete Lakelis Question
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/lakelisQuestion/:lakelisQuestionId"
  params:
    - name: lakelisQuestionId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
