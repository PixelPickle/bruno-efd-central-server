info:
  name: Game Login
  type: http
  seq: 12

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId/login"
  params:
    - name: gameId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "Password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
