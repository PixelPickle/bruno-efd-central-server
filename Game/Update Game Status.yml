info:
  name: Update Game Status
  type: http
  seq: 6

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId/gameStatus"
  params:
    - name: gameId
      value: "3"
      type: path
  body:
    type: text
    data: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
