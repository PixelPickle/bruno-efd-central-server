info:
  name: Update Game Admin Password
  type: http
  seq: 11

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId/adminPassword"
  params:
    - name: gameId
      value: "1"
      type: path
  body:
    type: text
    data: Password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
