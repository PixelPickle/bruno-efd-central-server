info:
  name: Update Game Pokemon Requirement
  type: http
  seq: 10

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId/pokemonRequirement"
  params:
    - name: gameId
      value: "1"
      type: path
  body:
    type: text
    data: "3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
