info:
  name: Delete Game
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId"
  params:
    - name: gameId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
