info:
  name: Update Game
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId"
  params:
    - name: gameId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "gameStatusId": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
