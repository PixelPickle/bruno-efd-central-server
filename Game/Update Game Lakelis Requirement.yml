info:
  name: Update Game Lakelis Requirement
  type: http
  seq: 7

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/game/:gameId/lakelisRequirement"
  params:
    - name: gameId
      value: "1"
      type: path
  body:
    type: text
    data: "2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
