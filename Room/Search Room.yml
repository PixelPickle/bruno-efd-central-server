info:
  name: Search Room
  type: http
  seq: 5

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/room"
  params:
    - name: NAME
      value: Chicken
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
