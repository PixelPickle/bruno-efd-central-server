info:
  name: Update Rfid Tag Type
  type: http
  seq: 3

http:
  method: PUT
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTagType/:rfidTagTypeId"
  params:
    - name: rfidTagTypeId
      value: "4"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "New Type"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
