info:
  name: Delete Rfid Tag Type
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTagType/:rfidTagTypeId"
  params:
    - name: rfidTagTypeId
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
