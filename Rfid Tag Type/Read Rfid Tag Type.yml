info:
  name: Read Rfid Tag Type
  type: http
  seq: 2

http:
  method: GET
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTagType/:rfidTagTypeId"
  params:
    - name: rfidTagTypeId
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
