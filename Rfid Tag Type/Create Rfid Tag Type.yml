info:
  name: Create Rfid Tag Type
  type: http
  seq: 1

http:
  method: POST
  url: "{{rootUrl}}/escapeFriendsDayCentral/api/rfidTagType"
  body:
    type: json
    data: |-
      {
        "name": "PASS_CARD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
